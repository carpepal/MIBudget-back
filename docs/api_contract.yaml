openapi: 3.0.3
info:
  title: MIBudget API
  description: API contract for the MIBudget application.
  version: 1.0.0
servers:
  - url: http://localhost:3000
    description: Local development server
paths:
  /example:
    get:
      summary: Example endpoint
      description: An example endpoint to demonstrate the API structure.
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    example: "Hello, world!"
components:
  schemas:
    